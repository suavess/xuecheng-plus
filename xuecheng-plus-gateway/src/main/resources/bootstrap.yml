spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: dev
        group: xuecheng-plus
      config:
        namespace: dev
        group: xuecheng-plus
        file-extension: yml
  profiles:
    active: dev
